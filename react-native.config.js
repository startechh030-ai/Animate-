/**
 * React Native CLI configuration.
 *
 * Registers React Native for Windows CLI commands (init-windows,
 * autolink-windows, run-windows) with the community CLI.
 */
const {commands} = require('@react-native-windows/cli');

module.exports = {
  commands,
};
